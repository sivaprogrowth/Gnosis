.DS_Store
node_modules
public
prof
tsconfig.tsbuildinfo
.obsidian
.quartz-cache
private/
.replit
replit.nix
.vercel
.env
.env.local
.env.*.local

# content/ is auto-regenerated each build by scripts/build-content.cjs from the
# canonical Gnosis vault (`$WIKI_SRC`). Only the directory placeholder is tracked.
# Hand-maintained pages live in content_overrides/ and are overlaid at build time.
content/*
!content/.gitkeep
