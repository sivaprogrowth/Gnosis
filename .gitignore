.DS_Store

# Obsidian vault config — per-machine. Each developer opens the vault
# and Obsidian regenerates its config. The Dataview plugin (2.4MB of
# vendor code) is fetched via the project setup, not tracked in git.
.obsidian/

# Marp slide export output — regenerated on demand by scripts/build-slides.sh
slides/dist/

*.swp
*.swo
.vscode/
